{
  "name": "calvoro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install:backend": "cd backend && npm install",
    "setup-db": "cd backend && node setup-database.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "replicate": "^1.4.0"
  }
}
